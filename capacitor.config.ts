import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uber-eats-customer-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
